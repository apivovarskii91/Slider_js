import SwipeCarousel from './carousel-swipe.js';

const carousel = new SwipeCarousel({
  // containerID:'#carousel',
  // slideID: 'item', 
  interval: 2000,
  isPlaying:false
});

carousel.initApp();
